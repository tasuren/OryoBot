グローバルチャット
複数チャンネルを繋げることができます。

`#gc make <NAME>`
<NAME>の名前でグローバルチャットを作成します。

`#gc connect <NAME>`
<NAME>の名前のグローバルチャットに接続します。

`#gc mode <TM>`
テーマを設定します。
<TM>にテーマの名前を入れます。
テーマリスト：
```
e : Embedで表示
wb : WebHookで表示(デフォルト) 
```
